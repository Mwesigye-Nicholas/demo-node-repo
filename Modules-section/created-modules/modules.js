// ** Modules **
// * CommonJs - every file is a modue (by default);
// *Modules - Encapsulated Code (only share what you what)

const names = require('./names');
const sayHi = require('./module-utils');
require('./mind-grunade');

const data = require('./modules2.js');
// !/const person = require('./modules2.js');

//console.log(data);
// !console.log(person);

// !console.log(names)
//sayHi('nicholas');
//sayHi(names.peter);
//sayHi(names.john);